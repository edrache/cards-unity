# Runtime

Runtime services that are not specific to combat or Unity UI live here.

Use this folder for game bootstrap helpers, run setup, and shared runtime utilities. Keep pure game state types in `Data` and combat math in `Combat`.
